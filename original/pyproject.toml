[project]
name = "skypulse"
version = "0.1.0"
description = "SkyPulse backend - outdoor activity score API"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
]

[project.scripts]
skypulse = "app:main"
